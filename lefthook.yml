# Lefthook git hooks — docs: https://lefthook.dev
# Install after clone: go tool lefthook install

pre-commit:
  commands:
    gocognit-directives:
      run: go run ./cmd/checkgocognit
    lint:
      run: go tool golangci-lint run

pre-push:
  commands:
    test:
      run: go test ./...
